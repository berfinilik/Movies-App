<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <LinearLayout
        android:id="@+id/linearLayout15"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/padding_medium"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageButton
            android:id="@+id/btnClose"
            android:layout_marginTop="@dimen/margin_top_medium"
            android:layout_marginStart="@dimen/margin_start_medium"
            android:layout_width="@dimen/button_icon_small"
            android:layout_height="@dimen/button_icon_small"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/close"
            android:src="@drawable/close"/>

        <View
            android:layout_width="@dimen/view_weight_width"
            android:layout_height="@dimen/view_weight_height"
            android:layout_weight="1" />

        <ImageButton
            android:id="@+id/btnShare"
            android:layout_width="@dimen/button_icon_medium"
            android:layout_height="@dimen/button_icon_medium"
            android:layout_marginTop="@dimen/margin_top_small"
            android:layout_marginEnd="@dimen/margin_end_small"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/share"
            android:src="@drawable/share" />
    </LinearLayout>

    <ImageView
        android:id="@+id/fullScreenImageView"
        android:layout_width="@dimen/view_weight_width"
        android:layout_height="@dimen/view_weight_height"
        android:adjustViewBounds="true"
        android:contentDescription="@string/fullscreen_image"
        android:scaleType="fitCenter"
        app:layout_constraintTop_toBottomOf="@id/linearLayout15"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>
